#include <stdio.h>
#include <stdlib.h>
#include <time.h>>

// Definindo a estrutura da lista
typedef struct Tno {

    int dado;
    struct no *proximo
} no;

// Definindo variável
typedef no *ptr_no;
